트위트 모델에 포린키로 author 을 넣으면 이 글에 한사람만 있다는 소리


class TweetModel(models.Model):
    class Meta:
        db_table = "tweet"

    author = models.ForeignKey(UserModel, on_delete=models.CASCADE)
    content = models.CharField(max_length=256)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)


터미널 설치랑 인터프리터 설치랑 차이??
로컬과 가상환경 차이 맞음??

redux??

makemigrations 랑 migrate 명령어 차이

파이썬 설치, path 변경?
가상환경은 처음 프로젝트 사용할 때 설치?
변수,상수는 알겠는데 리터럴??
지역변수,전역변수는 알겠는데 변수의 스코프는?
Mutable과 immutable 의 차이?
객체지향의 4대 원칙(캡슐화, 상속, 추상화, 다형성)
디버깅하는 법
1-11 인자 입력이 되는 부분이 안 보임?
- flake8 이랑 pre-commit hook 을 사용할 수도 있습니다. (커밋할 때마다 테스트, 린트 실행)
- pylint 도 편의상 생략했습니다.
- test coverage 를 보여주는 [coverage.py](http://coverage.py) 도 생략하였습니다. ([https://coverage.readthedocs.io/en/6.2/](https://coverage.readthedocs.io/en/6.2/))
- poetry add 시에 dev dependency 를 따로 나누지 않았습니다. 라이브 서버에는 코드 포매터가 필요 없곘죠?


수업에서는 `공식문서 읽기`를 강조합니다. mypy, black, 혹은 django-ninja 의 공식 문서를 원하는 만큼 읽어 오시는 것이 1주차 숙제입니다!


Django
- Django 가 Http Request 를 input 받아서, Http Response 를 output 한다는 점
- Django ORM 을 사용해서 Model 클래스를 정의하는 방법
- Django ORM 을 사용해서 기본적인 CRUD 를 수행하는 방법

Sql
- 간단한 CRUD 를 할 수 있어야 합니다.
- join 에 대해서 알고 계시고 inner join, left join, outer join 등, 각 join 의 차이점을 알고 계셔야 합니다.

-Authentication(인증) & Authorization(인가)
- Socket 통신
- UI (모든 API 는 swagger 로 테스트!!)





대표이사 주소변경등기
법인세 신고

여지껏 한 프로젝트들 한 폴더로 모아보기 나중에 꺼내서 코드 쓰기 용이

강의 심화까지 따라잡기 주말동안은 AWS연동이랑 GIT 강의 보기
자바스크립트 책 보면서 공부하기

알고리즘도 짬짬이 하기
프로그래머스 풀어보기

혼자 공부는 주로 인프런, 유튜브, 개발서적으로 학습
코테나 과제테스트 둘중 하나는 필수






